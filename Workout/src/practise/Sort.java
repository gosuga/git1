package practise;

import java.util.Comparator;

public class Sort implements Comparator <Cmp>  {
 	public int compare(Cmp o1,Cmp o2) {
 		if(o1.id>o1.id) {
 			return 1;
 		}else {
 			return -1;
 		}
 	}

// 	public class NameSort implements Comparator<Cmp> {
 		
// 		public int compare(Cmp o1,Cmp o2) {
// 		//	 return o2.name.compareToIgnoreCase(o1.name);
// 			 
// 		}
 	//}
}

